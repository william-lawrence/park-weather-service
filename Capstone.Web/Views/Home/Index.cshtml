@model IList<Park>
@{
    ViewData["Title"] = "Home Page";
}
@foreach (Park park in Model)
{
    <div class="home-park">
        <div class="home-park-img">
            <a asp-controller="Home" asp-action="Detail"><img src="~/images/parks/@(park.Code).jpg" /></a>
        </div>
        <div class="home-park-text">
            <div class="title"><a asp-controller="Home" asp-action="Detail">@park.Name</a></div>
            <div class="desciption">@park.Description</div>
        </div>
    </div>
}